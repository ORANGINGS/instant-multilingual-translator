# Changelog

## 1.4.3

- 修正 `G G` 快捷鍵的功能：現在切換的是整個擴充功能，不是重音強調模式。
- 關閉時會停止朗讀、關閉翻譯卡片，並阻止新的翻譯請求。
- 再次連按兩下 `G` 可重新開啟外掛；右上角會顯示目前狀態。
- 輸入框、文字區域、下拉選單與可編輯區域中不會觸發。

## 1.4.2

- 新增網頁快捷鍵：在非輸入欄位中連按兩下 `G`，即可切換英文重音強調模式。
- 快捷鍵切換後會在右上角顯示「已開啟／已關閉」提示。
- 在輸入框、文字區域、下拉選單與可編輯區域輸入時，不會觸發快捷鍵。
- 需要在 450ms 內完成第二次按鍵；按住 `G` 的自動重複不會觸發。

## 1.4.1

- 將「英文重音強調模式」改為設定頁中的獨立滑動開關。
- 重音模式預設關閉，只有使用者手動開啟時才會進行第二次慢速重播。
- 重音模式與「自動播放原文」互不綁定；手動按播放鍵時也會依開關狀態運作。
- 設定頁試播按鈕會同步示範目前的重音模式狀態。

## 1.4.0

- 新增「英文重音強調模式」。
- 英文單字先以正常語速播放，再於短暫停頓後以較慢語速與略高音調重播。
- 浮動翻譯視窗會顯示重音練習模式提示。
- 片語、句子與非英文內容仍維持單次正常播放，避免不必要的重複。
- 設定會透過 Chrome Storage 同步保存。

## 1.3.3

- 修正英文發音偶爾遺漏開頭音節，例如 `unforeseen` 只播放成 `foreseen`。
- `cancel()` 後不再立即呼叫 `speak()`，改為等待 140ms 讓 Chrome／Windows 語音引擎完成切換。
- 保留目前的 `SpeechSynthesisUtterance` 參考，避免播放期間被提早回收。
- 連續點擊播放時只保留最後一次請求，避免多個語音互相取消或重疊。

## 1.3.2

- 修正擴充功能更新或重新載入後，舊分頁出現 `Extension context invalidated` 與 `sendMessage` 錯誤。
- 線上查詢 Promise 立即掛上拒絕處理，避免 Chrome 記錄 `Uncaught (in promise)`。
- 偵測到舊版內容腳本失效時停止執行，並提示重新整理網頁。
- 捲動與視窗縮放監聽改為只在翻譯視窗開啟時註冊，關閉後立即移除，以降低網頁常駐負擔。

## 1.3.1

- 修正部分單字（例如 `solve`）顯示空白翻譯的問題。
- 將主要翻譯與多義詞查詢拆成獨立請求，避免詞典端點異常覆蓋翻譯結果。
- 線上服務回傳空值時改為顯示明確錯誤，不再留下空白區塊。
- 加入 UTF-8 請求參數與更完整的回傳格式解析。

## 1.3.0

- 新增設定視窗頂部的一鍵開啟／關閉切換按鈕。
- 關閉時立即關閉浮動翻譯視窗並停止語音播放。
- 關閉時不再執行線上翻譯請求。
- 工具列圖示在關閉狀態顯示 `OFF` 標記，並同步更新提示文字。
- 新增清楚的關閉狀態提示。

## 1.2.0

- 新增正式專案圖示與 Chrome 多尺寸圖示。
- 新增自動來源語言辨識。
- 新增英文、西班牙文及其他常用語言選項。
- 新增自訂目標翻譯語言。
- 浮動翻譯視窗可直接切換來源與目標語言。
- 新增跟隨系統、淺色與深色模式。
- 自動朗讀改為依來源語言選擇語音。
- 改善設定頁、浮動視窗與錯誤提示。
- 加入 GitHub README、隱私說明、貢獻指南與 Issue Template。

## 1.1.0

- 新增其他常見意思與詞性分類。
- 加入線上多義詞查詢。
- 改善本機翻譯與線上備援流程。

## 1.0.0

- 首次發布。
- 支援英文與繁體中文翻譯。
- 支援反白後自動朗讀與浮動翻譯視窗。
